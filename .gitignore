result
result-*
.direnv/
*.swp
.claude/settings.local.json
# Generated by git-hooks.nix on every devShell entry; pins a Nix store path.
.pre-commit-config.yaml
# Pi coding agent project-local state (input history, project settings) when
# running pi/spi from inside the repo.
.pi/
